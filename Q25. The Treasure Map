#include <iostream>
#include <vector>
using namespace std;

int main() {
    int n, m, target;
    cin >> n >> m;
    vector<vector<int>> matrix(n, vector<int>(m));
    for(int i = 0; i < n; i++)
        for(int j = 0; j < m; j++)
            cin >> matrix[i][j];
    cin >> target;

    bool found = false;
    for(int i = 0; i < n; i++) {
        for(int j = 0; j < m; j++) {
            if(matrix[i][j] == target) {
                found = true;
                break;
            }
        }
        if(found) break;
    }

    cout << (found ? "Yes" : "No");
    return 0;
}
